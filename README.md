# Aeroscan

```bash
npm init -y

npm install
npm install express
npm install @types/express
npm install mongodb

npm install ts-node --save-dev
npm install typescript --save-dev

npm ls -g --depth=0

node -v
tsc --init
```